NEXT_PUBLIC_SUPABASE_URL=https://yijzgsqmnlkirwyuquzc.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_GPIk5WKVPsScDtAeox4czg__t97rXB8

# Keep the actual password only in .env.local. Never commit it.
DATABASE_URL=postgresql://postgres.yijzgsqmnlkirwyuquzc:YOUR_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres.yijzgsqmnlkirwyuquzc:YOUR_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:5432/postgres

# The current resident auth remains unchanged in this limited migration.
AUTH_SECRET=replace-with-a-long-random-secret-at-least-32-characters

# Server-only. Use the Supabase Secret API key (or legacy service-role key) for
# private fire-report image uploads and short-lived signed download URLs. Never
# prefix this variable with NEXT_PUBLIC_ and never commit the real value.
SUPABASE_SECRET_KEY=sb_secret_replace_with_your_server_only_key

# Apply the emergency-workflow migration before enabling submissions. It creates
# the private fire-report-photos bucket (JPEG/PNG/WebP only, maximum 8 MB).
## Run once only after applying the BFP migration. Never commit real values.
PROVINCIAL_BFP_EMAIL=official-provincial-bfp@example.gov.ph
PROVINCIAL_BFP_NAME=Provincial BFP Administrator
PROVINCIAL_BFP_TEMP_PASSWORD=replace-with-a-unique-12-plus-character-temporary-password
