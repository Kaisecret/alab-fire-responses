# Use the Railway Postgres connection URL when running the app locally.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/railway

# Generate a private value with at least 32 characters. Never commit .env.local.
AUTH_SECRET=replace-with-a-long-random-secret-at-least-32-characters
