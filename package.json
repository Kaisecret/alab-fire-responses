{
  "name": "alab-fire-response",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix mainfile/alab-system",
    "dev": "npm run dev --prefix mainfile/alab-system",
    "build": "npm run build --prefix mainfile/alab-system",
    "start": "node scripts/start.mjs",
    "test": "node --test tests/*.test.mjs && npm test --prefix mainfile/alab-system"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
