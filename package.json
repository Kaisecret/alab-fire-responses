{
  "name": "alab-fire-response",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mainfile/alab-system"
  ],
  "scripts": {
    "build": "npm run build --workspace=alab-system",
    "start": "node scripts/start.mjs",
    "test": "node --test tests/*.test.mjs && npm test --workspace=alab-system"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "overrides": {
    "postcss": "8.5.24",
    "sharp": "0.35.3"
  }
}
